# MF — 작전주 시그널 분석 & 군중심리 교육 워크스페이스

> 이 파일은 프로젝트의 **헌법(constitution)** 이자 **라우팅 맵**이다.
> 작업할 때는 이 파일 + **해당 작업 디렉토리의 CLAUDE.md 하나만** 읽는다(토큰 효율 원칙).

---

## 1. 목적 (Purpose)

작전주(market-manipulation stock)가 "터지기" **이전**에 남기는 데이터적 흔적을 수집·분석해,
- 공통 **선행 시그널(leading signal)** 을 데이터로 검증하고,
- 이를 실사례에 접목해 **지인 교육용 자료**로 만들며,
- 그 과정에서 드러나는 **군중심리(crowd psychology)** 를 데이터 관점에서 설명한다.

**방어적·교육적 목적**이다. 조작을 *수행*하기 위한 도구가 아니라, 조작을 *식별·회피*하기 위한 도구다.

## 2. 윤리·면책 (Ethics & Disclaimer) — 위반 금지

- **공개 데이터(public data)만** 사용한다. 비공개·내부정보·해킹·스크래핑 약관 위반 금지.
- 실존 인물·세력을 **단정적으로 지목·비방하지 않는다.** 개인은 익명화/일반화한다.
- 산출물은 **교육·연구용**이며 **투자 자문·매매 신호가 아니다.** 모든 교육 자료에 이 면책을 명시한다.
- 본 분석을 **시세조종·선행매매·펌프앤덤프 실행**에 사용하지 않는다.

## 3. 절대 규칙: 모호한 것은 특정하지 않는다 (No Premature Specification)

이 프로젝트의 핵심 항목은 **"작전주의 조작적 정의(operational label)"** 다.
- 현재 상태: **v0.1 잠정 채택(PROVISIONAL)** — `PCR-MS`. → `labeling/label_definitions.md`(정본) 참조.
- **여전히 가설이다.** 미해결 질문 + 전향적 검증 전까지 확정 아님. 임계값·유니버스 경계 등은 임의 확정 금지.
- 정의를 바꾸거나 미해결 항목을 정할 땐 **사용자에게 질문**하고, `label_version`을 올려 결정 로그에 기록한다.

## 4. 방법론 원칙 (Methodology — 합의된 설계)

> 순진한 "터진 종목만 모아 공통점 찾기"는 **종속변수 선택 편향**으로 거의 확실히 틀린다.
> 아래 설계로 그 함정을 구조적으로 차단한다.

1. **케이스-컨트롤 (case-control)** — 양성(작전 의심) 사례마다 **매칭된 대조군**(같은 시총·업종·시기인데 안 터진 종목)을 둔다. 대조군 없는 시그널 주장은 무효.
2. **사건연구 (event study)** — 모든 시계열을 t0(터지는 시점) 기준으로 정렬, `t-N ~ t0` 윈도우에서 비교한다.
3. **변별력 측정** — "공통점 나열"이 아니라 **우도비(likelihood ratio)/lift**로 평가: *"이 시그널이 있으면 작전 확률이 몇 배 오르는가."*
4. **기저율(base rate) 명시** — `P(작전|시그널)`는 `P(시그널|작전)`와 다르다. 기저율 없이 결론 금지.
5. **as-of 동결 (no look-ahead)** — 모든 데이터에 **수집/공개 시점 타임스탬프**를 박는다. 사건 후 소급 해석된 정보로 과거를 오염시키지 않는다.
6. **전향적 검증 (prospective validation)** — 1개월 모니터링은 **out-of-sample 검증**이다. 과거 적합이 아니라 미래 예측으로 시그널을 최종 판정한다.
7. **과적합 경계** — 사례 수 대비 시그널 수를 통제. 사전 등록(pre-registration) + 홀드아웃.

## 5. 디렉토리 지도 (Routing Map)

| 디렉토리 | 목적 | 언제 여기로 |
|---|---|---|
| `data/price-volume/` | 가격·거래량·호가·회전율 시계열 | 수치 시계열 다룰 때 |
| `data/disclosure-news/` | 공시·뉴스·테마 이벤트 (근거자료 아카이브) | 이벤트/기사 다룰 때 |
| `data/sentiment/` | 종토방·SNS·텔레그램 등 여론 심리 | 커뮤니티/언어 데이터 다룰 때 |
| `data/flow-actors/` | 기관·외인·창구 등 수급 주체 | 매집/수급 주체 다룰 때 |
| `labeling/` | 양성 라벨 정의(미확정) + 대조군 매칭 | "무엇이 작전주인가" 정의·매칭 |
| `analysis/` | 시그널 추출·우도비·event study | 분석/모델링 할 때 |
| `validation/` | 전향적 검증·홀드아웃·백테스트 | 시그널 검증할 때 |
| `outputs/` | 교육 산출물(사례연구·체크리스트·발표·데이터셋) | 결과물 만들 때 |

각 디렉토리는 자체 `CLAUDE.md`에 **그 디렉토리에서만** 필요한 규칙/스키마를 담는다.

## 6. 데이터 표준 (전 디렉토리 공통)

- **자산군 태그**: 모든 레코드에 `asset_class ∈ {kr_stock, crypto, global_stock}`.
- **as-of 타임스탬프**: `observed_at`(내가 본 시점), `published_at`(원천 공개 시점) 둘 다 기록.
- **종목 식별자**: 가능한 한 표준 코드(예: KR 6자리, ticker, 거래소). 미정이면 자유 텍스트 + `id_unverified: true`.
- **출처(source)와 URL** 필수. 근거 없는 주장 금지.

## 7. 작업 흐름 (Pipeline)

```
수집(data/*) → 라벨링·대조군(labeling/) → 분석(analysis/) → 검증(validation/) → 교육 산출물(outputs/)
              ↑ 1개월 모니터링 = 전향적 검증 루프 →
```

현재 단계: **수집 + 라벨 정의 탐색(혼합 수동/자동, 자동화는 검증 이후)**.
